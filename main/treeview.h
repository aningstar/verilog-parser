// enum for the treeview model
enum {
    MODULE_NAME = 0,
    INSTANCE_NAME,
    INSTANCE_ENABLE,
    NUM_COLS
} ;
void create_and_fill_model ();
// Sets columns and store model for the treeview
void init_treeview ();
